# Dependencies
node_modules/

# Build output
dist/
build/
artifacts/

# Note: src/data/events.ts and public/guides/ are generated
# from the guide .md files by `bun run build:data`, but are committed so a fresh
# clone builds without that step. Re-run build:data after editing a guide.

# Archives / scratch
*.zip

# Local ballot databases — never commit someone's scores
*.sqlite
*.db

# OS / editor
.DS_Store
*.log
